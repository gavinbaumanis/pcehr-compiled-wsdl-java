<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright 2012 NEHTA
  Copyright 2021-2026 ADHA (Australian Digital Health Agency)
  SPDX: Apache-2.0; full text in LICENSE.txt at the repository root.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>au.gov.nehta</groupId>
    <artifactId>pcehr-compiled-wsdl</artifactId>
    <version>1.7.0-SNAPSHOT</version>
    <name>PCEHR Compiled WSDL library</name>
    <description>Java classes for B2B client development (Java 11 / Jakarta)</description>
    <url>https://github.com/AuDigitalHealth/pcehr-compiled-wsdl-java</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Admin Open Source</name>
            <email>admin.opensource@digitalhealth.gov.au</email>
            <organization>Australian Digital Health Agency</organization>
            <url>https://www.digitalhealth.gov.au/</url>
            <organizationUrl>https://www.digitalhealth.gov.au/</organizationUrl>
            <roles>
                <role>Admin</role>
            </roles>
            <timezone>UTC+10:00</timezone>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:${scm.repo.url}</connection>
        <developerConnection>scm:git:${scm.repo.url}</developerConnection>
        <url>${scm.repo.url}</url>
        <tag>${scm.tag}</tag>
    </scm>

    <issueManagement>
        <system>GitHub</system>
        <url>https://github.com/AuDigitalHealth/pcehr-compiled-wsdl-java/issues</url>
    </issueManagement>
    <ciManagement>
        <system>GitHub Actions</system>
        <url>https://github.com/AuDigitalHealth/pcehr-compiled-wsdl-java/actions</url>
    </ciManagement>

    <distributionManagement>
        <snapshotRepository>
            <id>central</id>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
        </snapshotRepository>
        <repository>
            <id>central</id>
            <url>https://central.sonatype.com</url>
        </repository>
    </distributionManagement>

    <properties>
        <maven.compiler.release>11</maven.compiler.release>
        <encoding>UTF-8</encoding>
        <project.build.sourceEncoding>${encoding}</project.build.sourceEncoding>
        <project.reporting.outputEncoding>${encoding}</project.reporting.outputEncoding>
        <project.resources.sourceEncoding>${encoding}</project.resources.sourceEncoding>
        <archetype.encoding>${encoding}</archetype.encoding>
        <gpg.skip>true</gpg.skip>
        <!-- SCM / maven-release-plugin (scm.repo.url is the only repo-specific Git value) -->
        <scm.repo.url>https://github.com/AuDigitalHealth/pcehr-compiled-wsdl-java.git</scm.repo.url>
        <scm.tag>HEAD</scm.tag>
        <release.localCheckout>true</release.localCheckout>
        <release.dryRun>false</release.dryRun>
        <release.goals>deploy</release.goals>
        <release.profiles>release</release.profiles>
        <release.tagNameFormat>@{project.artifactId}-@{project.version}</release.tagNameFormat>
        <release.pushChanges>true</release.pushChanges>
        <release.remoteTagging>true</release.remoteTagging>
        <release.scmCommentPrefix>[maven-release-plugin] </release.scmCommentPrefix>
        <!-- Consumers add com.sun.xml.ws:jaxws-rt 4.0.x at runtime; this JAR ships types + classpath WSDL only. -->
        <jakarta.xml.bind.api.version>4.0.5</jakarta.xml.bind.api.version>
        <jakarta.xml.ws.api.version>4.0.3</jakarta.xml.ws.api.version>
        <junit.version>4.13.2</junit.version>
        <plugin.clean.version>3.5.0</plugin.clean.version>
        <plugin.compiler.version>3.15.0</plugin.compiler.version>
        <plugin.surefire.version>3.5.5</plugin.surefire.version>
        <plugin.javadoc.version>3.12.0</plugin.javadoc.version>
        <plugin.source.version>3.4.0</plugin.source.version>
        <plugin.deploy.version>3.1.4</plugin.deploy.version>
        <plugin.release.version>3.3.1</plugin.release.version>
        <plugin.central.publishing.version>0.9.0</plugin.central.publishing.version>
        <plugin.gpg.version>3.2.8</plugin.gpg.version>
        <plugin.enforcer.version>3.6.2</plugin.enforcer.version>
        <skipTests>false</skipTests>
        <argLine>-Xmx1024m</argLine>
    </properties>

    <dependencies>
        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
            <version>${jakarta.xml.bind.api.version}</version>
        </dependency>
        <dependency>
            <groupId>jakarta.xml.ws</groupId>
            <artifactId>jakarta.xml.ws-api</artifactId>
            <version>${jakarta.xml.ws.api.version}</version>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <version>${plugin.clean.version}</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${plugin.enforcer.version}</version>
                <executions>
                    <execution>
                        <id>enforce-java11-jakarta</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireMavenVersion>
                                    <version>[3.6.3,)</version>
                                </requireMavenVersion>
                                <bannedDependencies>
                                    <excludes>
                                        <exclude>com.sun.xml.ws:webservices-rt</exclude>
                                        <exclude>com.sun.xml.ws:webservices-api</exclude>
                                        <exclude>com.sun.xml.ws:webservices-extra</exclude>
                                        <exclude>com.sun.xml.ws:webservices-extra-api</exclude>
                                        <exclude>javax.xml.bind:*</exclude>
                                        <exclude>javax.xml.ws:*</exclude>
                                        <exclude>javax.jws:*</exclude>
                                        <exclude>javax.xml.soap:*</exclude>
                                    </excludes>
                                    <message>Java 11 / Jakarta line: use jakarta.xml.bind and jakarta.xml.ws only. Consumers add EE4J jaxws-rt 4.0.x at runtime — not Metro webservices-* or legacy javax XML APIs.</message>
                                </bannedDependencies>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-gpg-plugin</artifactId>
                <version>${plugin.gpg.version}</version>
                <configuration>
                    <skip>${gpg.skip}</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>sign-artifacts</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>sign</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>${plugin.javadoc.version}</version>
                <configuration>
                    <verbose>false</verbose>
                    <quiet>false</quiet>
                    <failOnWarnings>true</failOnWarnings>
                    <doclint>all</doclint>
                    <detectOfflineLinks>false</detectOfflineLinks>
                    <encoding>${encoding}</encoding>
                    <source>${maven.compiler.release}</source>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>${plugin.source.version}</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>${plugin.deploy.version}</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <version>${plugin.central.publishing.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>central</publishingServerId>
                    <autoPublish>true</autoPublish>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>${plugin.release.version}</version>
                <configuration>
                    <useReleaseProfile>false</useReleaseProfile>
                    <releaseProfiles>${release.profiles}</releaseProfiles>
                    <localCheckout>${release.localCheckout}</localCheckout>
                    <dryRun>${release.dryRun}</dryRun>
                    <goals>${release.goals}</goals>
                    <tagNameFormat>${release.tagNameFormat}</tagNameFormat>
                    <pushChanges>${release.pushChanges}</pushChanges>
                    <remoteTagging>${release.remoteTagging}</remoteTagging>
                    <scmCommentPrefix>${release.scmCommentPrefix}</scmCommentPrefix>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${plugin.surefire.version}</version>
                <configuration>
                    <skipTests>${skipTests}</skipTests>
                    <encoding>UTF-8</encoding>
                    <enableAssertions>true</enableAssertions>
                    <argLine>${argLine}</argLine>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${plugin.compiler.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <proc>none</proc>
                    <compilerArgs>
                        <arg>-Xlint:unchecked</arg>
                    </compilerArgs>
                    <debug>true</debug>
                    <debuglevel>lines,vars,source</debuglevel>
                    <showDeprecation>true</showDeprecation>
                    <showWarnings>true</showWarnings>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <properties>
                <gpg.skip>false</gpg.skip>
            </properties>
        </profile>
    </profiles>

</project>
